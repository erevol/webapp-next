import React from 'react';
import Styled from '../../layout/Styled';

const rootClassnames = 'footer';

const Footer = () => {

  return (
    <Styled rootClassnames={rootClassnames}>
      Footer
    </Styled>
  );
};

export default Footer;
