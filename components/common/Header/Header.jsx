import React from 'react';
import Styled from '../../layout/Styled';

const rootClassnames = 'header';

const Header = () => {

  return (
    <Styled rootClassnames={rootClassnames}>
      Header
    </Styled>
  );
};

export default Header;
